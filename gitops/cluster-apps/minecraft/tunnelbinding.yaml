apiVersion: networking.cfargotunnel.com/v1alpha1
kind: TunnelBinding
metadata:
  name: minecraft
subjects:
  - kind: Service
    name: minecraft-server
    spec:
      fqdn: play.bazza.dev
      protocol: udp
      target: udp://minecraft-server.minecraft.svc.cluster.local:25565
tunnelRef:
  kind: ClusterTunnel
  name: orion
  disableDNSUpdates: false
